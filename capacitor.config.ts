import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.huynhbalong.gig_os',
  appName: ' Personal Gig OS',
  webDir: 'dist'
};

export default config;
